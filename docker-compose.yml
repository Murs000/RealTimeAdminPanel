# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP.NET Core service.

version: '3.4'

services:
  realtimeadminpanel:
    container_name: realtimeadminpanel-web
    image: realtimeadminpanel
    build:
      context: .
      dockerfile: RealTimeAdminPanel/Dockerfile
    ports:
      - 5228:5228
